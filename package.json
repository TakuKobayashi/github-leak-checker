{
  "name": "github-leak-checker",
  "version": "0.0.6",
  "description": "This is the check-tools if the resource is leaked to Github",
  "main": "dist/index.js",
  "author": "TakuKobayashi",
  "license": "MIT",
  "bin": {
    "github-leak-checker": "./bin/github-leak-checker.js"
  },
  "scripts": {
    "build": "tsc -p tsconfig.json",
    "format": "prettier --config .prettierrc --write 'src/**/*.{ts,js}'",
    "docs": "typedoc --out docs/ && touch docs/.nojekyll",
    "jest": "jest -c jest.config.js",
    "ts-node": "ts-node"
  },
  "dependencies": {
    "@octokit/rest": "^18.12.0",
    "chalk": "^5.0.1",
    "commander": "^9.2.0",
    "simple-git": "^3.7.1",
    "typescript": "^4.6.4"
  },
  "devDependencies": {
    "@types/jest": "^27.5.0",
    "@types/node": "^17.0.31",
    "documentation": "^13.2.5",
    "dotenv": "^16.0.0",
    "jest": "^28.1.0",
    "prettier": "^2.6.2",
    "ts-node": "^10.7.0",
    "typedoc": "^0.22.15",
    "typings": "^2.1.1"
  }
}
